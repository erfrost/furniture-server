name: Build and deploy to server

on:
  push:
    branches:
      - master

jobs:
  docker:
    name: Create docker image and push to registry, then start image
    runs-on: ubuntu-latest
    steps:
      - name: Pull image and run container with .env variables from secrets
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: 5.101.50.171
          username: root
          password: 702ray2s71
          port: 22
          script: cd /root/app && git pull origin master && docker stop app || true && docker rm app || true && docker build -t app . || true && docker run -d -p 80:8080 -v /root/images:/images -v --name app --rm app
